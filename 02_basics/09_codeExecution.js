// Js is a single threaded language

// Interview important stuff
 // Javascript execution context
    // Global execution context -> this
    // Function execution context
    // Eval execution context (Mostly appears in mongooose) // Property of global execution context 

// JS runs in two phases
    // Memory creation phase/ creation phase (only memory allocation takes place for variables)
    // Execution phase (code is being executed like mathematical expressions)